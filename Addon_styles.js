import { WorkTheme } from '@pega/cosmos-react-core';

export const card = {
  largemetricsHeader: {
    paddingTop: `calc(${WorkTheme.base.spacing} * 2)`,
    paddingBottom: `calc(${WorkTheme.base.spacing} * 2)`,
    color: WorkTheme.base.colors.slate['dark']
  }
};
